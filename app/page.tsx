import Post from "./components/Post.js";

export default function Home() {
  const handleClick = () => {
    <Post />
  };

  return (
    <main>
      <Post />
      <button onClick={handleClick} />
    </main>
  );
}